# OpenClaw .env example
#
# Quick start:
# 1) Copy this file to `.env` (for local runs from this repo), OR to `~/.openclaw/.env` (for launchd/systemd daemons).
# 2) Fill only the values you use.
# 3) Keep real secrets out of git.
#
# Env-source precedence for environment variables (highest -> lowest):
# process env, ./.env, ~/.openclaw/.env, then openclaw.json `env` block.
# Existing non-empty process env vars are not overridden by dotenv/config env loading.
# Note: direct config keys (for example `gateway.auth.token` or channel tokens in openclaw.json)
# are resolved separately from env loading and often take precedence over env fallbacks.

# -----------------------------------------------------------------------------
# Gateway auth + paths
# -----------------------------------------------------------------------------
# Recommended if the gateway binds beyond loopback.
OPENCLAW_GATEWAY_TOKEN=change-me-to-a-long-random-token
# Example generator: openssl rand -hex 32

# Optional alternative auth mode (use token OR password).
# OPENCLAW_GATEWAY_PASSWORD=change-me-to-a-strong-password

# Optional path overrides (defaults shown for reference).
# OPENCLAW_STATE_DIR=~/.openclaw
# OPENCLAW_CONFIG_PATH=~/.openclaw/openclaw.json
# OPENCLAW_HOME=~

# Optional: import missing keys from your login shell profile.
# OPENCLAW_LOAD_SHELL_ENV=1
# OPENCLAW_SHELL_ENV_TIMEOUT_MS=15000

# -----------------------------------------------------------------------------
# Model provider API keys (set at least one)
# -----------------------------------------------------------------------------
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GEMINI_API_KEY=...
# OPENROUTER_API_KEY=sk-or-...

# Optional additional providers
# ZAI_API_KEY=...
# AI_GATEWAY_API_KEY=...
# MINIMAX_API_KEY=...
# SYNTHETIC_API_KEY=...

# -----------------------------------------------------------------------------
# Channels (only set what you enable)
# -----------------------------------------------------------------------------
# TELEGRAM_BOT_TOKEN=123456:ABCDEF...
# DISCORD_BOT_TOKEN=...
# SLACK_BOT_TOKEN=xoxb-...
# SLACK_APP_TOKEN=xapp-...

# Optional channel env fallbacks
# MATTERMOST_BOT_TOKEN=...
# MATTERMOST_URL=https://chat.example.com
# ZALO_BOT_TOKEN=...
# OPENCLAW_TWITCH_ACCESS_TOKEN=oauth:...

# -----------------------------------------------------------------------------
# Tools + voice/media (optional)
# -----------------------------------------------------------------------------
# BRAVE_API_KEY=...
# PERPLEXITY_API_KEY=pplx-...
# FIRECRAWL_API_KEY=...

# ELEVENLABS_API_KEY=...
# XI_API_KEY=...  # alias for ElevenLabs
# DEEPGRAM_API_KEY=...

# =============================================================================
# OracLaw - Oracle AI Database Storage
# =============================================================================
# OracLaw extends OpenClaw with persistent memory powered by Oracle AI Vector
# Search.  Choose ONE of two modes:
#
#   freepdb  – local Docker container (Oracle Database Free)
#   adb      – Oracle Autonomous Database on OCI (cloud)
#
# The sidecar handles all database ops; the LLM (Anthropic by default) is
# configured above in "Model provider API keys".

# Oracle Database Mode
# ORACLE_MODE=freepdb

# ---- Option A: FreePDB (local Docker) ----
# Spins up with `docker compose up oracle-freepdb oraclaw-service`
# ORACLE_USER=oraclaw
# ORACLE_PASSWORD=OracLaw2024
# ORACLE_HOST=localhost
# ORACLE_PORT=1521
# ORACLE_SERVICE=FREEPDB1

# ---- Option B: Autonomous Database (OCI Cloud) ----
# Wallet-less TLS connections (thin driver) – paste the full DSN descriptor.
# Credentials from oracle-ai-developer-hub/apps/agentic_rag/config.yaml style.
# ORACLE_MODE=adb
# ORACLE_USER=ADMIN
# ORACLE_PASSWORD=Welcome12345*
# ORACLE_DSN=(description= (retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=1522)(host=adb.us-phoenix-1.oraclecloud.com))(connect_data=(service_name=g2f4dc3e5463897_mern_tpurgent.adb.oraclecloud.com))(security=(ssl_server_dn_match=yes)))
#
# If your ADB requires a wallet (mTLS), also set:
# ORACLE_WALLET_PATH=/path/to/wallet
# ORACLE_WALLET_PASSWORD=wallet-secret

# Connection Pool
# ORACLE_POOL_MIN=2
# ORACLE_POOL_MAX=10

# In-database ONNX Embedding Model
# ORACLE_ONNX_MODEL=ALL_MINILM_L12_V2

# OracLaw Sidecar Service
# ORACLAW_SERVICE_PORT=8100
# ORACLAW_SERVICE_TOKEN=          # Set to enable bearer token auth on sidecar API
